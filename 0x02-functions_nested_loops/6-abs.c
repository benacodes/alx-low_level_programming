#include "main.h"
/**
 * _abs - Function that computes absolute value of integer
 *
 * @a: integer number
 *
 * Return: "a" for absolute value. -1 for otherwise
 */

int _abs(int a)
{
	if (a >= 0)
	{
	return (a);
	}
	else
	{
	return (-1);
	}
}
