#include "main.h"
/**
 * _abs - Function that computes absolute value of integer
 *
 * @a: integer number
 *
 * Return: "a" for absolute value. "-a" for otherwise
 */

int _abs(int a)
{
	if (a >= 0)
	{
	return (a);
	}
	else
	{
	return (-a);
	}
}
